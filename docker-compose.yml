
services:
  dev-env:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./projects:/root  # Mount the host project directory to /workingdir in the container
    ports:
      - "8080:8080"
    stdin_open: true
    tty: true

